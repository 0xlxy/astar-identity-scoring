upstream client {
    root /usr/share/nginx/html;
}

upstream server {
    server server:8000;
}

server {
    listen 80;

    location / {
        proxy_pass http://client;
    }

    location /api {
        rewrite /api/(.*) /$1 break;
        proxy_pass http://server;
    }
}